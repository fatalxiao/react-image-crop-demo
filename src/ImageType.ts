/**
 * @file ImageType.ts
 */

export type ImageType = {
    id: string
    label: string
    aspect: number
    recommendedWidth: number
    recommendedHeight: number
    minWidth: number
    minHeight: number
}
